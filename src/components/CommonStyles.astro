<style is:global>
  @tailwind components;

  @layer components {
    section {
      @apply bg-white text-blue-900;
    }

    h2 {
      @apply mt-5 text-3xl font-light tracking-tight leading-tight;
    }

    .subtitle {
      @apply text-lg font-light leading-relaxed mb-4;
    }

    .feature-item {
      @apply text-left p-4;
    }

    .feature-header {
      @apply flex items-center mb-4;
    }

    .feature-icon {
      @apply w-8 h-8 object-contain mr-4;
    }

    .feature-title {
      @apply text-xl font-normal text-counterparty-red m-0;
    }

    .feature-description {
      @apply text-sm leading-relaxed font-light;
    }
  }
</style>
